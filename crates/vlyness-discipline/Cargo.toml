[package]
name = "vlyness-discipline"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "VLYNESS connection discipline: budget manager, exponential backoff, blackhole detector (design §5, §9)."

[dependencies]
rand.workspace = true
