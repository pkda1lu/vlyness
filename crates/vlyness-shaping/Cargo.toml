[package]
name = "vlyness-shaping"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "VLYNESS traffic shaping: length sampler, cadence generator, symmetrizer (design §8)."

[dependencies]
rand.workspace = true
thiserror.workspace = true
