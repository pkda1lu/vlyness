[package]
name = "vlyness-profile"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "VLYNESS Profile model and coherence validator (design §1): a single legend from which every observable is derived."

[dependencies]
serde.workspace = true
serde_json.workspace = true
